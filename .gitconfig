[user]
        name = Benjamin Chait
        email = benjamin@chait.co
[filter "media"]
        required = true
        clean = git media clean %f
        smudge = git media smudge %f
[credential]
        helper = osxkeychain
[alias]
        co = checkout
        br = branch
        ci = commit
        st = status
        unstage = reset HEAD --
        log = log -l HEAD
        visual = !gitk
[filter "hawser"]
        clean = git hawser clean %f
        smudge = git hawser smudge %f
        required = true
